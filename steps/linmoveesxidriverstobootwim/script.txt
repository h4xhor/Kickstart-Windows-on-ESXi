cd "{ksAttuneBaseDir}/build-{kickstartedNode.fqn}"

ATTUNE_DRIVERS="WinPE_BootImageDir/attune_drivers"

mkdir --parents --verbose "${ATTUNE_DRIVERS}"

DRIVERS="Drivers"
mv --verbose ${DRIVERS}/* "${ATTUNE_DRIVERS}"

rmdir --verbose "${DRIVERS}"